<h2>Edit Device Pin</h2>

<%= render "form.html", Map.put(assigns, :action, config_device_pin_path(@conn, :update, @device_id, @device_pin)) %>

<span><%= link "Back", to: config_device_pin_path(@conn, :index, @device_id) %></span>
